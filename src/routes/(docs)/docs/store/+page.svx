# Cache

`@aperta/cache` is a simple logging library. It is designed to be:

- Fast
- Simple
- Framework agnostic

Installation is easy with `npm`:

```cmd
npm i @aperta/cache
```

## something

### something
